<?phpinclude_once LOGIC . 'properties.php';if ( $properties->rowCount() > 0 ) {	while ( $property = $properties->fetchObject() ) {		include LOGIC . 'main_image.php';		include INC . 'property_preview.php';	}} else {	// empty result set	$empty_error_msg = ""; // empty_error_msg is undefined		if ( $template['name'] == 'default' ) {		$empty_error_msg = "Attualmente non è disponibile nessun annuncio. Torna presto a trovarci!";	} else	if ( $template['name'] == 'ricerca' ) {		$empty_error_msg = 'Nessun annuncio corrispondente alla ricerca effettuata. Prova ad effettuare una nuova ricerca, oppure <a href="'.ROOT.'">Torna alla Home</a>.';	}	echo "<p>$empty_error_msg</p>";}